{
  "name": "monorepo",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently --kill-others --raw \"npm run dev --prefix apps/api/web\" \"npm run dev --prefix apps/api\" \"npm run dev --prefix apps/api/pocketbase\"",
    "build": "concurrently --raw \"npm run build --prefix apps/api/web\"",
    "start": "concurrently --kill-others --raw \"npm run start --prefix apps/api\" \"npm run start --prefix apps/api/pocketbase\"",
    "lint": "concurrently --raw \"npm run lint --prefix apps/api/web\" \"npm run lint --prefix apps/api\"",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,css,md}\""
  },
  "devDependencies": {
    "concurrently": "^9.1.2",
    "eslint-config-prettier": "^10.1.8"
  },
  "workspaces": [
    "apps/*"
  ]
}
